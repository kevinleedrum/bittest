---
description: "An example UI widget"
labels: ["label1", "label2", "label3"]
---

import { Widget } from "./widget";

This is simply a React-based counter that uses the `mx-button` and `mx-input` components from the MoxiWorks Design System.

```js live
<Widget />
```
